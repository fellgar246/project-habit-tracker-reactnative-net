# Copy to .env and replace with your machine's LAN IP so Expo Go can reach the API.
# Find your IP: ipconfig (Windows) or ifconfig / ip addr (macOS/Linux)
EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api/v1
